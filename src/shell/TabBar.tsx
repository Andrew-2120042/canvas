/** Dashboard tab + one tab per open file. Populated in 1.8. */
export function TabBar() {
  return (
    <div className="tabbar">
      <span className="region-stub">Tab bar</span>
    </div>
  );
}

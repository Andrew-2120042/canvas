import { Toolbar } from "./Toolbar";

/** Infinite viewport. Pan/zoom in 1.2, primitives from 1.3. */
export function CanvasRegion() {
  return (
    <main className="canvas-region">
      <Toolbar />
    </main>
  );
}
